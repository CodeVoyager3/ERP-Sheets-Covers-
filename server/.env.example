# Direct PostgreSQL connection string.
# For Prisma Postgres: Prisma Console -> your database -> "Direct connection" URL
# (starts with postgresql://...db.prisma.io...&sslmode=require).
# Do NOT use the prisma+postgres:// Accelerate URL here — the app uses the pg driver adapter.
DATABASE_URL="postgres://f875df53c522adb47bbe002ec6cdda34e1fb09be343d65a1ab2c409b05f01d28:sk_TKl_bLhTrR7lBQJkJZgp_@pooled.db.prisma.io:5432/postgres?sslmode=require"

# Long random string used to sign JWTs. Generate e.g. with: openssl rand -base64 48
JWT_SECRET="H;o28t*Gc08_,dCMLeFD=GaJy##Y.%#KAY*o#Gas:A6"

# Comma-separated list of allowed browser origins (your Vercel frontend URLs).
# Leave unset in development to allow all.
CORS_ORIGIN="https://your-app.vercel.app"

# Optional; the platform provides PORT automatically in production.
# PORT=5000
